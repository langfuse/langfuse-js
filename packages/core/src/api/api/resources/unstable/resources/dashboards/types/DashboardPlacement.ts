/**
 * This file was auto-generated by Fern from our API Definition.
 */

import * as LangfuseAPI from "../../../../../index.js";

/**
 * A tile on the dashboard's 12-column grid. `x`/`y` are the tile's
 * top-left cell (0-based; `y` grows downward), `width`/`height` its size
 * in cells. The UI default tile is 6x6 (half width). Overlapping tiles
 * are not rejected; prefer appending below existing tiles (or omit the
 * position on create to let the server do it).
 */
export type DashboardPlacement =
  | LangfuseAPI.unstable.DashboardPlacement.Widget
  | LangfuseAPI.unstable.DashboardPlacement.Preset;

export namespace DashboardPlacement {
  export interface Widget extends LangfuseAPI.unstable.WidgetPlacement {
    type: "widget";
  }

  export interface Preset extends LangfuseAPI.unstable.PresetPlacement {
    type: "preset";
  }
}
