/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * @example
 *     {
 *         fromStartTime: "2024-01-15T09:30:00Z"
 *     }
 */
export interface ListExperimentItemsRequest {
  /**
   * Comma-separated list of field groups to include. Available groups:
   * `core`, `dataset`, `io`, `metadata`, `itemMetadata`,
   * `experimentMetadata`, `scores`. If omitted, `core,dataset` is
   * returned.
   */
  fields?: string;
  /** Number of experiment items to return per page. Maximum 100, default 50. */
  limit?: number;
  /** Number of scores to return per experiment item when `fields=scores` is requested. Maximum 50, default 50. */
  scoreLimit?: number;
  /** Versioned base64url cursor from the previous response page. */
  cursor?: string;
  /** Retrieve only experiment items started on or after this datetime. */
  fromStartTime: string;
  /** Retrieve only experiment items started before this datetime. */
  toStartTime?: string;
  /** Comma-separated list of experiment IDs. */
  experimentId?: string;
  /** Comma-separated list of experiment names. */
  experimentName?: string;
  /** Comma-separated list of experiment item IDs. */
  experimentItemId?: string;
  /** Comma-separated list of dataset IDs. */
  datasetId?: string;
  /**
   * JSON string containing an array of structured filter conditions.
   * Supported columns are `experimentId`, `experimentName`,
   * `experimentItemId`, and `datasetId`.
   */
  filter?: string;
}
