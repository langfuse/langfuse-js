/**
 * This file was auto-generated by Fern from our API Definition.
 */

/**
 * @example
 *     {
 *         fromStartTime: "2024-01-15T09:30:00Z"
 *     }
 */
export interface ListExperimentsRequest {
  /**
   * Comma-separated list of field groups to include. Available groups:
   * `core`, `metadata`, `scores`. If omitted, `core` is returned.
   */
  fields?: string;
  /** Number of experiments to return per page. Maximum 100, default 50. */
  limit?: number;
  /** Number of scores to return per experiment when `fields=scores` is requested. Maximum 50, default 50. */
  scoreLimit?: number;
  /** Versioned base64url cursor from the previous response page. */
  cursor?: string;
  /** Retrieve only experiments on or after this datetime. */
  fromStartTime: string;
  /** Retrieve only experiments before this datetime. */
  toStartTime?: string;
  /** Comma-separated list of experiment IDs. */
  id?: string;
  /** Comma-separated list of experiment names. */
  name?: string;
  /** Comma-separated list of dataset IDs. */
  datasetId?: string;
  /**
   * JSON string containing an array of structured filter conditions.
   * Supported columns are `id`, `name`, and `datasetId`.
   */
  filter?: string;
}
